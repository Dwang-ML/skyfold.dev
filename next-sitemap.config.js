/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://skyfold.dev',
  generateRobotsTxt: true,
  outDir: './public', // ensures sitemap & robots.txt end up in public/
}
